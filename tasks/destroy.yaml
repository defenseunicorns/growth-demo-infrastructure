tasks:
  - name: base
    description: "Remove the base UDS BTB Bundle"
    actions:
      - description: "Remove the base UDS BTB Bundle"
        cmd: uds remove uds-bundle-base-*.tar.zst --confirm --no-progress

  - name: swf
    description: "Remove the SWF UDS BTB Bundle"
    actions:
      - description: "Remove the SWF UDS BTB Bundle"
        cmd: uds remove uds-bundle-swf-*.tar.zst --confirm --no-progress
